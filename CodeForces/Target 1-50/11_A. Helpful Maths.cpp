#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin >> s;
    
    sort(s.begin(),s.end());
    cout << s;
    
    // for (int i = 0; i < s.size(); i+2)
    // {
    //     s.erase(i+1);
    // }
   
    
    
    return 0;
}