#include<bits/stdc++.h>
using namespace std;
int main()
{
    int x;
    int sum = 0;
    sum = x+x;
    //Number of iteration=3-->O(3)

    int n;
    cin >> n;
    for(int i = 0; i< n; i++){
        int y;
        y = 5;
        y++;
    }
    //O(5) + O(3*N)--> O(N)
    return 0;
}