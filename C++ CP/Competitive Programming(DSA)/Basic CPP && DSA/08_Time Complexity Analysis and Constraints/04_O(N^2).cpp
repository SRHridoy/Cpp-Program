#include<bits/stdc++.h>
using namespace std;
int main()
{
//N < 100000
//a[i] < 100000
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            //cin >> a[i]
        }
        
    }
//O(1)+O(1)+O(n^2) = O(n^2)    

    return 0;
}