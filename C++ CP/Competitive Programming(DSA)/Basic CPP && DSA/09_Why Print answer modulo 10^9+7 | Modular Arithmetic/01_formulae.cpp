#include<bits/stdc++.h>
using namespace std;
int main()
{
/*
    1.Plus-->(a + b) % M = ((a % M) + (b % M)) % M
    2.Multiplication-->(a * b) % M = ((a % M) * (b % M)) % M
    3.Minus-->(a - b) % M = ((a % M) - (b % M) + M) % M
    4.Division-->(a / b) % M = ((a % M) * (b^-1) % M) % M
*/
    return 0;
}