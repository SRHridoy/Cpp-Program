#include<bits/stdc++.h>
#define int   long long
using namespace std;

vector<int>NGE(vector<int>v){
    
}

signed main(){
    int n; cin >> n;
    vector<int>v(n);
    for(int i = 0; i < n; i++){
        cin >> v[i];
    }
    vector<int>nge=NGE(v);
}